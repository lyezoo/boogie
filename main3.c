#include "lh_proto.h"

int		main(int ac, char **av)
{
	ft_lstnew(av[1], atoi(av[2]));

//	ft_lstdelone(, &fct_inexistante);

//	ft_lstdel(, &fct_inexistante);

//	ft_lstadd(, );

//	ft_lstiter(, &fct_inexistante);

//	ft_lstmap(, &fct_inexistante);

	return (0);
}
